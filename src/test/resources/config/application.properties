# driver settings
browser=${browser}
browser.config=${browser.config}
# start page settings
site.url=${site.url}
# remote settings
remote=${remote}
hub_url=${hub_url}
# proxy settings
proxy=${proxy}
proxy_config=${proxy_config}
################
## COMMON
################
server.port=8090
dp.url=${dp.url}